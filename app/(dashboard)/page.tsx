import { UserButton } from "@clerk/nextjs";
import Image from "next/image";

const DashboardPage = () => {
  return (
    <div>
      new app changes
      <UserButton />
    </div>
  );
};

export default DashboardPage;
